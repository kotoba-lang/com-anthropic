{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([anthropic.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'anthropic.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
